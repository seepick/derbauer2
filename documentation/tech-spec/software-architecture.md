# Software Architecture

* Domain-drive approach of using packages.
* Dependency Injection with Koin to decouple components.
* Massive use of interfaces to attach "capabilities" to entities.
    * Roles as in `CanStore`, `Storageable`, etc. 
