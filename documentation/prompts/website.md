# Implement a basic project website

* write all files to the `/docs` folder
* a single HTML file named `index.html` is sufficient
* the website should include:
    * a project title
    * a brief description of the project
    * two links, one for windows and the other for mac.